import { COPILOT_QUESTIONS } from "@/lib/copilot-benchmarks"

export const dynamic = "force-dynamic"

const refusals = ["Amazon refresh tokens and encrypted secrets", "Copilot audit trail raw reads", "User identity and membership dumps", "Direct writes or multi-statement SQL", "Unfiltered marts without tenant-safe views", "Sleep/timeout/comment-smuggling SQL"]

export default function CopilotPage() {
	return <div className="space-y-5"><div className="rounded-xl border border-blue-200 bg-blue-50 p-4"><p className="text-xs font-semibold uppercase tracking-wide text-blue-700">System Copilot</p><h1 className="mt-1 text-xl font-semibold text-slate-950">Ask account questions safely</h1><p className="mt-2 max-w-3xl text-sm text-slate-700">The backend copilot is read-only, tenant-scoped and audited. It can answer from approved views and package recommendations as proposals, but it cannot apply Amazon changes. Numeric/write-like commands must still go through the approval queue.</p></div><section className="rounded-lg border bg-white p-4"><h2 className="font-medium">Ask a question</h2><p className="mt-1 text-sm text-slate-600">Runtime chat execution is intentionally kept behind the copilot service boundary. This surface shows the supported benchmark prompts and safety contract so the web/mobile UI can wire execution without inventing prompts.</p><form className="mt-4 grid gap-3"><textarea name="question" rows={4} placeholder="Example: Kaun se keywords ne paisa jalaya magar order nahi diya?" className="rounded border px-3 py-2 text-sm"/><div className="flex flex-wrap gap-2"><button disabled className="rounded bg-slate-300 px-4 py-2 text-sm text-white">Run through copilot service</button><span className="self-center text-xs text-slate-500">Disabled until the API boundary exposes the audited copilot runner.</span></div></form></section><section className="rounded-lg border bg-white p-4"><div className="mb-3 flex flex-wrap items-center justify-between gap-2"><h2 className="font-medium">Supported benchmark questions</h2><span className="rounded bg-slate-100 px-2 py-1 text-xs text-slate-600">{COPILOT_QUESTIONS.length} tested prompts</span></div><div className="grid gap-3 md:grid-cols-2">{COPILOT_QUESTIONS.map(q => <article key={q.key} className="rounded border border-slate-100 bg-slate-50 p-3"><div className="flex items-center justify-between gap-2"><span className="text-xs font-medium uppercase tracking-wide text-slate-500">{q.intent}</span><span className={q.tier === "T2" ? "rounded bg-amber-100 px-2 py-0.5 text-xs text-amber-800" : "rounded bg-green-100 px-2 py-0.5 text-xs text-green-800"}>{q.tier}</span></div><p className="mt-2 text-sm font-medium text-slate-900">{q.question}</p><p className="mt-1 text-sm text-slate-600">{q.questionUr}</p></article>)}</div></section><div className="grid gap-4 lg:grid-cols-2"><section className="rounded-lg border bg-white p-4"><h2 className="font-medium">Safety contract</h2><ul className="mt-3 list-disc space-y-2 pl-5 text-sm text-slate-600"><li>Connects with <code>DATABASE_URL_COPILOT</code>, not owner/app credentials.</li><li>Boot-checks read-only PostgreSQL role before every query.</li><li>Runs <code>set_tenant()</code> in the same transaction as the query.</li><li>Caps result rows and audits terminal outcome.</li><li>T2 recommendations write nothing; humans approve actions separately.</li></ul></section><section className="rounded-lg border bg-white p-4"><h2 className="font-medium">Must refuse</h2><ul className="mt-3 list-disc space-y-2 pl-5 text-sm text-slate-600">{refusals.map(item => <li key={item}>{item}</li>)}</ul></section></div></div>
}
