import Link from "next/link"
import { SettingsCard, SettingsHeading, StatusPill } from "@/components/settings-ui"

const rows = [
	{ area: "Enterprise readiness", backend: "Documented: production hardening, Amazon evidence, write-back safety and ops blockers", frontend: "Coverage matrix links to the readiness audit; final production approval remains separate", href: "https://github.com/abaanshujat212-beep/amz-command-center/blob/main/docs/enterprise-readiness-audit.md", status: "partial" },
	{ area: "SP-API Sales & Traffic", backend: "Implemented: client, raw load, parser, sandbox seed/setup, pipeline runs", frontend: "Visible in Amazon settings, Command Center product analytics, history/run evidence", href: "/settings/amazon", status: "covered" },
	{ area: "Ads ingestion", backend: "Implemented: report orchestration, raw landing tables, HTTP client seam", frontend: "Campaign dashboard, search terms, placement surfaces and Ads approval/testing wizard; approval still externally blocked", href: "/settings/amazon", status: "partial" },
	{ area: "Tenant access scopes", backend: "Implemented: per-member access_scopes and custom tenant role templates", frontend: "Members UI lets owners/admins assign feature scopes and owners save custom role templates", href: "/settings/members", status: "covered" },
	{ area: "Tenant picker", backend: "Implemented: session active_tenant_id plus membership lookup", frontend: "Login no longer asks for tenant ID; single tenant auto-enters and multi-tenant users get a client picker", href: "/login", status: "covered" },
	{ area: "Approval decision API", backend: "Implemented: tenant-scoped approve/reject contract reusing the same guarded decision logic as UI", frontend: "Approval queue UI and API clients share RBAC, expiry, finding refusal, support checks and audit trail", href: "/api/approvals", status: "covered" },
	{ area: "API boundary", backend: "Implemented: tenant-scoped JSON read routes plus guarded approval decision route; other mutating routes remain blocked", frontend: "Web/mobile clients can consume stable read endpoints and approve/reject through the audited contract", href: "/api/dashboard", status: "partial" },
	{ area: "Platform API keys", backend: "Implemented: tenant-scoped hashed API key table with revoke support", frontend: "API key settings can create read keys, show prefixes and revoke active keys", href: "/settings/api-keys", status: "covered" },
	{ area: "MCP support", backend: "Deferred: not required for Amazon MVP; needs tenant-safe MCP auth and read-only tool contract", frontend: "MCP readiness page documents inbound/outbound sequence and no-secrets/no-write constraints", href: "/settings/mcp", status: "partial" },
	{ area: "System Copilot", backend: "Implemented: read-only runner, SQL guard, system map, benchmark questions and audit/refusal contract", frontend: "Copilot page posts through tenant-authenticated prompt boundary and validates no-write runner payloads", href: "/copilot", status: "partial" },
] as const
function tone(status: string): { ready: boolean; label: string; cls: string } { if (status === "covered") return { ready: true, label: "Frontend covered", cls: "tone-good" }; return { ready: false, label: "Needs follow-up", cls: "tone-warn" } }
export default function CoveragePage() { return <div className="space-y-6"><SettingsHeading title="Backend → frontend coverage" description="A working map of backend features and where operators can see or use them in the app."/><SettingsCard title="Coverage matrix"><div className="overflow-x-auto"><table className="w-full text-sm"><thead><tr className="border-b text-left text-xs uppercase tracking-wide text-slate-500"><th className="p-3">Feature</th><th className="p-3">Backend status</th><th className="p-3">Frontend surface</th><th className="p-3">Status</th></tr></thead><tbody>{rows.map(row => { const t = tone(row.status); return <tr key={row.area} className="border-b align-top last:border-0"><td className="p-3 font-medium">{row.area}</td><td className="p-3 text-slate-600">{row.backend}</td><td className="p-3 text-slate-600">{row.frontend}<div><Link href={row.href} className="text-xs text-blue-700 hover:underline">Open surface →</Link></div></td><td className={`p-3 ${t.cls}`}><StatusPill ready={t.ready}>{t.label}</StatusPill></td></tr> })}</tbody></table></div></SettingsCard></div> }
