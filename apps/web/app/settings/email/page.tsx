import { SettingsCard, SettingsHeading, StatusPill } from "@/components/settings-ui"
export default function EmailSettingsPage(){const resend=Boolean(process.env.RESEND_API_KEY);const smtp=Boolean(process.env.SMTP_HOST);return <div><SettingsHeading title="Email services" description="Transactional delivery for alerts, invitations and reports."/><div className="grid gap-4 md:grid-cols-2"><SettingsCard title="Resend" aside={<StatusPill ready={resend}>{resend?"Configured":"Not configured"}</StatusPill>}><p className="text-sm text-slate-600">Recommended transactional email provider. Configure <code>RESEND_API_KEY</code> and a verified sender domain on the server.</p></SettingsCard><SettingsCard title="SMTP" aside={<StatusPill ready={smtp}>{smtp?"Configured":"Not configured"}</StatusPill>}><p className="text-sm text-slate-600">Connect an existing mail server using <code>SMTP_HOST</code>, username and server-side password.</p></SettingsCard></div></div>}
