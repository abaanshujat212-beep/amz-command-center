import { SettingsCard, SettingsHeading, StatusPill } from "@/components/settings-ui"
export default function ApiKeysPage(){return <div><SettingsHeading title="API keys" description="Credentials used by integrations and programmatic clients."/><SettingsCard title="Platform API keys" aside={<StatusPill ready={false}>Not enabled</StatusPill>}><p className="text-sm text-slate-600">This deployment does not issue public tenant API keys yet. Provider credentials are managed as protected server environment variables and are never exposed in the browser.</p><div className="mt-4 rounded-lg bg-slate-50 p-3 text-xs text-slate-500">Amazon refresh tokens use the encrypted vault. AI and email provider keys must be supplied through the deployment secret manager.</div></SettingsCard></div>}
