import type { PoolClient } from "pg"
import { mart, query } from "./db"

export type PlacementRow = { campaign_id:string; campaign_name:string; placement:string; placement_api_enum:string|null; placement_modifier_pct:number|null; impressions:number; clicks:number; cost:number; sales:number; orders:number; acos:number|null; ctr:number|null; cvr:number|null; spend_share:number|null; cvr_index:number|null; break_even_acos:number|null; economics_incomplete:boolean }
export async function placementPerformance(client: PoolClient, days = 30, limit = 100): Promise<PlacementRow[]> { return query<PlacementRow>(client, `select campaign_id, campaign_name, placement, placement_api_enum, max(placement_modifier_pct) as placement_modifier_pct, sum(impressions) as impressions, sum(clicks) as clicks, sum(cost) as cost, sum(attributed_sales_7d) as sales, sum(attributed_orders_7d) as orders, case when sum(attributed_sales_7d)>0 then sum(cost)/sum(attributed_sales_7d) end as acos, case when sum(impressions)>0 then sum(clicks)::numeric/sum(impressions) end as ctr, case when sum(clicks)>0 then sum(attributed_orders_7d)::numeric/sum(clicks) end as cvr, case when sum(sum(cost)) over (partition by campaign_id)>0 then sum(cost)/sum(sum(cost)) over (partition by campaign_id) end as spend_share, avg(cvr_index) as cvr_index, min(break_even_acos) as break_even_acos, bool_or(economics_incomplete) as economics_incomplete from ${mart("mart_ppc_placement_daily")} where is_settled and report_date >= current_date - ($1::int || ' days')::interval group by campaign_id,campaign_name,placement,placement_api_enum order by sum(cost) desc limit $2`, [days, limit]) }

export type LlmSpend = { currency:string; spend:number; tokens:number; calls:number; last_call_at:string|null }
export async function llmSpendThisMonth(client: PoolClient): Promise<LlmSpend[]> { return query<LlmSpend>(client, `select currency, spend, tokens, calls, last_call_at::text from v_llm_spend_this_month order by spend desc`) }
